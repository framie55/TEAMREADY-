# TeamReady Known Issues

_Status as of Phase 1 inspection, 2026-09-07. All items below are reported, not fixed — nothing has been changed. Update this file's status column as each item is addressed or re-confirmed. Severity: Critical (possible data loss/security problem/app unavailable), High (important workflow broken or wrong information), Medium (reliability/usability/automation), Low (cleanup/optional)._

| # | Severity | Issue | Evidence | Affects | Status |
|---|---|---|---|---|---|
| 1 | Critical | Weekly Last Man Standing pick-reminder SMS appears to be failing in production with a Twilio authentication error | `LmsError` entity, real unresolved entries dated 2026-09-01 and 2026-09-04, source `sendWeeklyPickSms` | LMS entrants who rely on the reminder to make their pick in time | Open — awaiting manager decision |
| 2 | Critical | LMS integrity alert unresolved: "20 paid entries, 21 picks submitted" | `LmsError`, source `lmsFridayIntegrityAlert`, `resolved:false` | LMS competition accuracy, entrants' money | Open — awaiting manager decision |
| 3 | Critical | No row-level security / access rules declared on any of the ~95 data entities, including player personal data (phone, DOB, login PIN) and all financial records | Checked every `base44/entities/*.jsonc` for `rls`/permission fields — none found anywhere | Player privacy, financial data | Open — needs a decision on who should access what before any fix |
| 4 | Critical | Confirmed duplicate player record: two records for James Dickinson ("Jiffy") — one archived, one active | `Player` entity, live data | Stats/identity accuracy | Open — awaiting manager confirmation before any merge |
| 5 | Critical | Unexplained extra "Paul Griffith" record alongside the two legitimate Paul Griffiths | `Player` entity, live data | Stats/identity accuracy | Open — awaiting manager confirmation |
| 6 | High | Zero automated tests exist anywhere in the app | `package.json` has no test script, no test framework installed, no test files found | Whole app — no safety net against regressions in stats/payments/selection logic | Open |
| 7 | High | Season stat totals on `Player` are written from three different places (`LiveMatchCentre` full-time increment, `finaliseMatch`, `recalculatePlayerStats`); the first is not idempotent and doesn't create the matching `PlayerMatchStat` row on its own | `src/pages/LiveMatchCentre.jsx` `handleFullTime`, `base44/functions/finaliseMatch/entry.ts`, `base44/functions/recalculatePlayerStats/entry.ts` | Season stats accuracy if "Finalise Match" is ever skipped or run twice | Open |
| 8 | High | AI-generated match analysis auto-writes onto `MatchReport` with no human approval step, then feeds season-average "intelligence" screens | `base44/functions/generateMatchReportAi/entry.ts`, displayed in `MatchIntelligence.jsx`/`ClubBrain.jsx` | Risk of AI content being treated as verified fact | Open |
| 9 | High | "Add Player" screen tells the admin an invite SMS is sent automatically; it isn't — a separate manual "Invite" button is required | `src/components/squad/AddPlayerModal.jsx` (UI copy) vs actual invite trigger in `Squad.jsx` | Any player added recently may not actually have been invited | Open |
| 10 | High | No duplicate-name check when creating a new player | `AddPlayerModal.jsx` `handleSave` — only checks `full_name` is non-empty | Root cause behind issues #4/#5 continuing to happen | Open |
| 11 | High | `Fixture.result_home/result_away` and `MatchReport.home_score/away_score` are two separate fields with no sync guarantee | `base44/entities/Fixture.jsonc`, `base44/entities/MatchReport.jsonc` | Result accuracy if the two are ever edited independently | Open |
| 12 | Medium | Three separate, unreconciled money ledgers: `Payment` (subs/fines), rewards levy system (`RewardsFund`/`RewardsLevyTransaction`), and `FundraisingDonation` | Entity schemas; `src/components/lms/LmsManagePanel.jsx` manually applies a levy without checking an actual `Payment` was made for that entry | Financial accuracy, double-count risk in the LMS rewards flow specifically | Open |
| 13 | Medium | No `.env.example` or documented list of required secrets (Twilio, Stripe, OneSignal, API-Football, admin phone) | No such file found in the repo | Operational risk if credentials ever need rotating/reconfiguring by someone other than the original builder | Open |
| 14 | Medium | Git commit history is 788 commits but ~85% are generic ("File changes"/"External agent changes"), making it hard to audit what changed and why | `git log` in the Base44 sandbox | Ability to trace when/why a bug was introduced | Open — informational, not fixable retroactively |
| 15 | Medium | Two orphaned pages with no route pointing to them: `OAuthConsent.jsx`, `Register.jsx` | Cross-referenced every `src/pages/*.jsx` against `src/App.jsx` routes | Dead code, minor confusion risk | Open |
| 16 | Medium | ~200 pages are all statically imported with no code-splitting; several of the largest pages (`SponsorHub.jsx`, `CommercialHub.jsx`, `MatchIntelligence.jsx`, `LiveMatchCentre.jsx`) use almost no memoization despite 20-40+ render loops each | `src/App.jsx` imports; grep for `useMemo`/`React.memo`/`useCallback` usage | Mobile load time and in-app responsiveness, especially pitch-side on matchday | Open |
| 17 | Medium | Several "AI"-branded features don't actually call an AI model (only `ClubBrain.jsx` does) | `ManagerDNA.jsx`, `AiTeamBuilder.jsx` (via `ManagerAnalysis.jsx`), `PlayerInsights.jsx` — no LLM calls found in any of these | Expectation-setting only, not a functional bug | Open |
| 18 | Low | 314 lint errors, all unused imports, auto-fixable | `npm run lint` output | Code cleanliness only | Open |
| 19 | Low | 648 type-check errors, concentrated in public/supporter/player-portal pages | `npm run typecheck` output | Latent bug risk in newer, less-used page groups | Open |
| 20 | Low | Stale `caniuse-lite`/browserslist data | Build output notice | Cosmetic | Open |

_Add new rows as issues are found; move status to "Fixed" (with date and change reference) or "Won't fix (reason)" as they're resolved — never delete a row outright, so the history of what was known and when is preserved._
