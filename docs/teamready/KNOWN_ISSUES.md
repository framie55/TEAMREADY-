# Known Issues

_This is now the canonical known-issues list, carried forward in full from `docs/TEAMREADY_KNOWN_ISSUES.md` (superseded, kept for history) plus everything found since. Status as of 2026-09-09. Severity: Critical (possible data loss/security problem/app unavailable), High (important workflow broken or wrong information), Medium (reliability/usability/automation), Low (cleanup/optional)._

| # | Severity | Issue | Evidence | Affects | Status |
|---|---|---|---|---|---|
| 1 | Fixed | Weekly LMS pick-reminder SMS was failing in production with a Twilio authentication error | `LmsError`, entries 2026-09-01/04, source `sendWeeklyPickSms` | LMS entrants | **Fixed 2026-09-09** — Twilio was deliberately discontinued (cost), not a technical fault. `SMS_ENABLED = false` guard added; logs one clear summary instead of one failure per person. **Follow-up open:** same pattern in ~14 other functions, see row 21. |
| 2 | Resolved | LMS integrity alert: "20 paid entries, 21 picks submitted" | `LmsError`, source `lmsFridayIntegrityAlert` | LMS accuracy | **Explained 2026-09-09** — a phantom `LmsEntry` against an already-archived duplicate player, never actually paid, never actually picked, already manually voided by Paul on 2026-09-03. No money or real entrant ever at risk. |
| 3 | Critical | No row-level security on any of the ~95 entities, including player personal data and all financial records | Checked every `base44/entities/*.jsonc` | Player privacy, financial data | Open — needs a decision on who should access what before any fix |
| 4 | Low | Duplicate empty James Dickinson player record | `Player` `6a9d2f18586e6e627cf46f91` | None — confirmed zero references anywhere | Manager approved deletion 2026-09-09; not yet deleted — no delete tool available to Claude, needs manual removal via Squad |
| 5 | Low | Extra "Paul Griffith" record alongside the two legitimate Paul Griffiths | `Player` `6a6e2dee326d3a4cb774fe3c` | A harmless, already-voided `LmsEntry`+`LmsPick` pair still reference it | **Closed 2026-09-09** — Paul confirmed: leave as is, no further investigation. There are two Paul Griffiths (Griff, goalkeeper; Griffo, outfield), both correctly separate and active; the archived "Paul Griffith" record stays archived, untouched. |
| 6 | High | Zero automated tests exist anywhere in the app | No test script/framework/files | Whole app — no regression safety net | Open |
| 7 | High | Season stats on `Player` written from 3 places; the live-match increment isn't idempotent and doesn't create the matching `PlayerMatchStat` row itself | `LiveMatchCentre.jsx handleFullTime`, `finaliseMatch`, `recalculatePlayerStats` | Season stats accuracy | Reduced risk 2026-09-09 since Finalise now runs automatically on Full Time, but the underlying 3-writer structure is unchanged |
| 8 | High | AI-generated match analysis auto-writes with no human approval, feeds season-average "intelligence" screens | `generateMatchReportAi`, `MatchIntelligence.jsx`/`ClubBrain.jsx` | Risk of AI content treated as fact | Open |
| 9 | High | "Add Player" screen claims an invite SMS sends automatically; it doesn't — separate manual button required | `AddPlayerModal.jsx` vs `Squad.jsx` | Any recently-added player may not have been invited | Open |
| 10 | High | No duplicate-name check when creating a new player | `AddPlayerModal.jsx handleSave` | Root cause behind issues #4/#5 recurring | Open |
| 11 | High | `Fixture.result_home/result_away` and `MatchReport.home_score/away_score` are two separate fields, no sync guarantee | Entity schemas | Result accuracy if edited independently | Open |
| 12 | Medium | Three separate, unreconciled money ledgers (`Payment`, LMS rewards levy, `FundraisingDonation`) | Entity schemas, `LmsManagePanel.jsx` | Financial accuracy, double-count risk in LMS rewards flow | Open |
| 13 | Medium | No `.env.example` or documented list of required secrets | No such file in repo | Operational risk on credential rotation | Open |
| 14 | Medium | Git history is 788+ commits, ~85% generic ("File changes"/"External agent changes") | `git log` in Base44 sandbox | Hard to audit when/why something changed | Open — informational |
| 15 | Medium | Two orphaned pages with no route: `OAuthConsent.jsx`, `Register.jsx` | Cross-ref against `src/App.jsx` | Dead code | Open |
| 16 | Medium | ~200 pages statically imported, no code-splitting; several large pages barely memoized | `src/App.jsx`, grep for `useMemo`/`React.memo` | Mobile load time, matchday responsiveness | Open |
| 17 | Medium | Several "AI"-branded features don't call an AI model (only `ClubBrain.jsx` does) | `ManagerDNA.jsx`, `AiTeamBuilder.jsx`, `PlayerInsights.jsx` | Expectation-setting only | Open |
| 18 | Low | 314 lint errors, all unused imports, auto-fixable | `npm run lint` | Cleanliness only | Open |
| 19 | Low | 648 type-check errors, concentrated in public/supporter/player-portal pages | `npm run typecheck` | Latent bug risk in less-used pages | Open |
| 20 | Low | Stale `caniuse-lite`/browserslist data | Build output | Cosmetic | Open |
| 21 | High | ~14 functions still call the discontinued Twilio account | See `AUTOMATION_REGISTER.md` | Availability chasing, matchday reminders, chairman/MOTM comms going nowhere | Open — awaiting channel-replacement decision |
| 22 | Fixed | Matchday Studio was a public, unauthenticated page that could independently record goals/cards/subs into real match data | `MatchdayStudio.jsx` had its own full recording engine, zero auth checks, unguarded route | Match data integrity; open to public tampering | **Fixed 2026-09-09** — confirmed unused for recording, all recording controls/handlers/modals removed; now purely read-only |
| 23 | Fixed | "Finalise Match" was a separate, easy-to-miss second step after "Confirm Full Time" — a match could look finished while stats stayed unfinalised | `LiveMatchCentre.jsx` | Season stats accuracy | **Fixed 2026-09-09** — Full Time now auto-finalises; manual re-finalise button remains for corrections |
| 24 | Fixed | "Paul Dimond" (database) vs "Paul Diamond" (manager's own sent FA email and FA's official registered squad list) — spelling conflict | `Player` `6a362b0234d01f9e95620923` | Could cause an FA-registration name mismatch | **Fixed 2026-09-09** — confirmed "Diamond" is correct against the FA's own registered list; `full_name` corrected. |
| 25 | Medium | Matchday programme "Results This Season" panel mixed up two similarly-named opponents (Darlington Railway Athletic vs Darlington DSRM) — wrong result attached to wrong date | Verified against real `Fixture` data 2026-09-07 | Accuracy of a document sent to sponsors/players | Open — root cause (live bug vs. one-off manual slip) not yet traced, see `PROGRAMME_WORKFLOW.md` |
| 26 | Medium | Manager's own MOTM pick (used in the FA email) has no dedicated storage — only the player vote (`MotmVote`) is saved | `MatchReport.motm_player_id`/`motm_player_name` exist in schema but unused by any page | Manager's pick only exists in whatever email/document it was typed into | Open |
| 27 | Fixed | "James Shikle" was missing a letter — FA's registered spelling is "Shickle" | `Player` `6a38ca0a21cb430084f8f8b7` | FA-registration accuracy | **Fixed 2026-09-09**, confirmed against FA's registered squad list |
| 28 | Fixed | Second real duplicate-player pattern found: an empty "Stephen Halliday" (correct spelling, zero stats, created 6 Sept — same pattern as the earlier James Dickinson duplicate) alongside the real record misspelled "Stephen Haliday " | `Player` `6a9d2f18586e6e627cf46f93` (empty) vs `6a38ca0a21cb430084f8f8b6` (real, 2 real `PlayerMatchStat` rows) | None — resolved before causing confusion | **Fixed 2026-09-09** — empty duplicate archived (with a note), real record's spelling corrected to "Halliday", his current injury status also logged per Paul's confirmation |
| 29 | Won't fix — not a database issue | "Corby" nickname investigation | Initially flagged as a possible identity conflict (nickname "Corby" on `Player` "Anthony Richardson" vs the club programme referencing "Corby" as "the manager") | Documentation only | **Resolved 2026-09-09**: the club has **two co-managers** — Paul Frame ("Framie", also plays) and Anthony Richardson ("Corby", does not play). The database was correct throughout; the error was in Claude's own `APP_OVERVIEW.md` draft, which wrongly stated "Manager: Paul Frame, known as Corby." Corrected. |
| 30 | Fixed | Club badge existed in three different versions across the app/assets: `Club.badge_url`, a different hardcoded URL used in-app, and a third "Grindon Broadway ... EST 2021" version seen on a kit mockup | `Club` entity, ~30 hardcoded references in code, kit mockup photo supplied by Paul 2026-09-07 | Public website badge accuracy, club history accuracy | **Fixed 2026-09-08** — Paul confirmed the badge already showing live in the app (the hardcoded `grindonboardinnbadge.png`, "Grindon Board Inn," EST. 1995) is correct. Updated `Club.badge_url` (was pointing at a different, unused file) to match it, and set `Club.name` (previously blank) to "Grindon Board Inn Over 40s FC." The ~30 hardcoded code references already pointed at the right file, so no code changes were needed — this was purely a stale-database-field fix. |
| 31 | Fixed (was Critical) | Public/supporter pages fetched the full `Player` record client-side with no field whitelisting; since no entity has row-level security, private fields (`login_pin`, `otp_code`, `invite_token`, `phone`, `email`, `date_of_birth`, emergency contacts) were present in the raw browser network response on `/public/squad`, `/supporter/squad`, `/supporter/stats`, `/supporter/match-centre`, `/supporter` (SupporterHome) | Confirmed via code inspection 2026-09-07; direct `base44.entities.Player.filter(...)` calls in all 5 pages | Every active player's personal data, readable by any anonymous visitor via browser devtools | **Fixed 2026-09-07** — Paul approved the patch. New backend function `getPublicPlayerData` (mirrors the existing `getGuestDashboardData` safe-aggregator pattern) explicitly whitelists only public-safe fields; all 5 pages now call it via `base44.functions.invoke()` instead of querying `Player` directly. `npm run build` and targeted `eslint` both clean (only pre-existing, unrelated unused-import errors present). This closes the immediate exposure; Known Issues #3 (no row-level security at all, any entity) remains open as the underlying root cause. |
| 32 | Fixed (was High) | `Sponsor` records carry undeclared fields beyond the formal schema (`address`, `contact_name`, `phone`, `email`, `contract_notes`) — the public sponsor strip fetched the full record client-side, same category of issue as #31 | Confirmed via schema/sample-data inspection 2026-09-07 | Sponsor contact privacy on any page reading `Sponsor` directly | **Fixed 2026-09-08** — `getPublicSponsorData` now used by every genuinely public/supporter page that reads `Sponsor`: `PublicHome.jsx`, `PublicSponsors.jsx`, `SupporterSponsors.jsx`. Checked the remaining `Sponsor`-reading pages (`Sponsors.jsx`, `Dashboard.jsx`'s `SponsorStrip`, `LmsWinner.jsx`, `media/SponsorGraphics.jsx`) — all sit behind `ProtectedRoute`+`AdminGuard` (staff login required), so they legitimately need the full record for managing sponsors and were correctly left untouched. |

| 33 | Fixed (was High) | `MatchReport.jsx`'s single save button posted the match write-up straight to the public news feed with zero human review step, conflicting with the club's own "never let a match go official without review" rule | `MatchReport.jsx handleSave`, confirmed via code inspection 2026-09-08 | Any match report ever saved before this fix went instantly public, unreviewed | **Fixed 2026-09-08** — split into "Save Match Result" (unchanged, instant — Paul's own direct input) and a new separate review panel with an editable preview and its own "Publish to Website" button. Nothing reaches the public `ClubNews` feed until that second, explicit step. |
| 34 | Fixed (was Critical, launch blocker) | The bare domain root path `/` was not in `App.jsx`'s public-paths allowlist — any anonymous visitor at a custom domain pointed at this app's root would have been redirected to the staff login screen instead of the public website | `App.jsx` `AuthenticatedApp`, confirmed via code inspection 2026-09-08 while explaining the go-live process to Paul | Would have made the entire public website appear broken/inaccessible the moment a custom domain was connected | **First fix (2026-09-08) was incomplete** — only verified via `curl` status codes, which can't see client-side routing; the real second domain hit the exact bug in a real browser. **Properly fixed 2026-09-08 (later same day)** — root cause was that the redirect lived inside an `authError`-gated block that never runs when the app's Base44 visibility is set to Public (see `DECISIONS_AND_CORRECTIONS.md`). Moved the redirect to key on `isAuthenticated` directly instead. Confirmed this time by Paul testing the live custom domain in a real browser. |

_Add new rows as issues are found; move status to "Fixed" (with date and change reference) or "Won't fix (reason)" as they're resolved — never delete a row outright, so the history of what was known and when is preserved._
